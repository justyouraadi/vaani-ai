[project]
name = "tbb-vaani-ai"
version = "0.1.0"
description = "Self-hosted real-time streaming voice AI agent (Hindi/Hinglish) with barge-in"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]