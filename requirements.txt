fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=1.26,<2
PyYAML>=6.0
openai>=1.40
faster-whisper>=1.1.0
silero-vad>=5.0.1
huggingface_hub>=0.24
pytest>=8.0
pytest-asyncio>=0.24

# TTS (server B). Install CUDA torch first:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# then:
#   pip install coqui-tts